import 'package:flutter/material.dart';

class AppColors {
  static const MaterialColor themeColor = Colors.blue; // 改变这里
  static const Color yellowColor = Colors.blue.shade400; // 改变这里
  static const Color grayColor = Color(0xFF2d2d2d);
  static const Color darkSurfaceColor = Color(0xff373737);

  static const Color whiteColor = Colors.white;
  static const MaterialColor greenColor = Colors.blue; // 改变这里
}
